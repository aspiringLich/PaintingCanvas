package paintingcanvas.drawable;

public interface Interactable {
    /**
     * Check if the element is currently being hovered over.
     * @return true if the element is hovered, false otherwise
     */
    boolean hovered();
}
